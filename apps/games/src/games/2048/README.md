# 2048

4×4 の盤面で同じ数字のタイルをくっつけ、2048 のタイルを作るパズル。

- 矢印キー / WASD / スワイプで全タイルを一方向に寄せる。同じ数字は 1 回の移動で 1 度だけ合体する
- 2048 を作ると「続ける」か「もう一度」を選べる。どの向きにも動かせなくなったら終わり
- ベストスコアは localStorage の `2048:best` に残す

ルールは `lib/game.ts` に React から切り離して書いてある。
