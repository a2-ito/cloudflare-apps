// src/types/tools.ts
export type PasswordGeneratorMessages = {
  name: string;
  description: string;
  generate: string;
  copy: string;
  length: string;
  lowercase: string;
  uppercase: string;
  numbers: string;
  symbols: string;
};

export type UnixTimeConverterMessages = {
  name: string;
  description: string;
  unix: string;
  datetime: string;
  milliseconds: string;
  now: string;
  utc: string;
  local: string;
};
