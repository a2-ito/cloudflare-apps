{
  "name": "app-portal",
  "version": "0.1.0",
  "private": true,
  "description": "app.a2ito.work のトップページ。配下のアプリへのリンクを並べる静的ページ",
  "scripts": {
    "dev": "wrangler dev",
    "cf:build": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "cf:deploy": "wrangler deploy"
  },
  "dependencies": {
    "wrangler": "^4.136.2"
  }
}
