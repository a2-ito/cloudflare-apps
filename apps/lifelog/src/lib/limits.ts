/**
 * 入力の上限。フォーム（クライアント）とサーバーアクションの両方から読むため、
 * "use server" のファイルには置けない（非同期関数以外を export できない）。
 */

/** 1 件の本文。長い日記でも収まり、貼り付け事故で D1 の行を膨らませない程度 */
export const MAX_BODY_LENGTH = 10_000;

/** 一覧の 1 ページに出す件数 */
export const PAGE_SIZE = 30;
